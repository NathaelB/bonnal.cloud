---
import { Icon } from "@iconify/react";

interface Project {
  titleKey: string;
  descKey: string;
  title: string;
  description: string;
  icon: string;
  tags: string[];
  href: string;
  color?: string;
  isMore?: boolean;
}

const projects: Project[] = [
  {
    titleKey: "projects.ferriskey.title",
    descKey: "projects.ferriskey.description",
    title: "FerrisKey",
    description: "IAM platform built in Rust: OIDC, multi-tenancy, a policy engine, and Kubernetes-native, for systems where correctness isn't optional.",
    icon: "mdi:shield-key-outline",
    tags: ["Rust", "IAM", "OIDC", "Kubernetes"],
    href: "https://github.com/ferriskey/ferriskey",
    color: "#f97316",
  },
  {
    titleKey: "projects.ferriscord.title",
    descKey: "projects.ferriscord.description",
    title: "Ferriscord",
    description: "An open-source, Discord-like communication platform: servers, channels, and realtime chat, built in Rust.",
    icon: "mdi:forum-outline",
    tags: ["Rust", "Realtime", "ScyllaDB", "Kafka"],
    href: "https://github.com/ferrislabs/ferriscord",
    color: "#8b5cf6",
  },
  {
    titleKey: "projects.mestier.title",
    descKey: "projects.mestier.description",
    title: "Mestier",
    description: "An open-source SaaS platform that centralizes core business tools: CRM, ERP, HR, and messaging, in one system.",
    icon: "mdi:office-building-outline",
    tags: ["Rust", "SaaS", "CRM/ERP"],
    href: "https://github.com/ferrislabs/mestier",
    color: "#d946ef",
  },
  {
    titleKey: "projects.ferriskv.title",
    descKey: "projects.ferriskv.description",
    title: "FerrisKV",
    description: "A distributed, multi-tenant key-value database in Rust, built to expose wire-compatible protocols, Postgres among them, on top of its storage engine.",
    icon: "mdi:database-sync-outline",
    tags: ["Rust", "Distributed", "Multi-tenant"],
    href: "https://github.com/ferrislabs/ferriskv",
    color: "#14b8a6",
  },
  {
    titleKey: "projects.more.title",
    descKey: "projects.more.description",
    title: "And many more...",
    description: "Discover all my open-source projects on GitHub.",
    icon: "mdi:github",
    tags: [],
    href: "https://github.com/nathaelb",
    isMore: true,
  },
];

const TEXTURE = "background-image: linear-gradient(to right, var(--color-border) 1px, transparent 1px), linear-gradient(to bottom, var(--color-border) 1px, transparent 1px); background-size: 34px 34px; mask-image: radial-gradient(ellipse 70% 60% at 85% 0%, black, transparent 72%); -webkit-mask-image: radial-gradient(ellipse 70% 60% at 85% 0%, black, transparent 72%);";
---

<section id="projects" class="relative py-24 sm:py-32">
  <div class="max-w-6xl mx-auto px-6">
    <!-- Header -->
    <div class="mb-12" data-reveal="fade-up">
      <p class="text-sm font-semibold text-primary uppercase tracking-widest mb-3" data-t="projects.label">
        Projects
      </p>
      <h2 class="text-3xl font-bold text-foreground sm:text-4xl" data-t="projects.title">
        Open source products, not demo code
      </h2>
      <p class="mt-4 text-lg text-muted-foreground max-w-2xl" data-t="projects.description">
        Each project is a system decision made concrete: an architecture choice, a protocol design, years of upkeep.
      </p>
    </div>

    <!-- Connected matrix, per-project accent -->
    <div
      class="grid grid-cols-1 gap-px overflow-hidden rounded-[4px] border border-border bg-border sm:grid-cols-2 lg:grid-cols-3"
      data-reveal="fade-up"
      data-reveal-delay="100"
    >
      {projects.map((project) => (
        project.isMore ? (
          <a
            href={project.href}
            target="_blank"
            rel="noopener noreferrer"
            class="proj-more group relative flex items-center justify-center overflow-hidden bg-background p-6 text-center transition-colors duration-300 hover:bg-muted/30"
          >
            <div class="flex flex-col items-center gap-3">
              <span class="flex size-10 items-center justify-center rounded-[3px] bg-muted/40 text-muted-foreground">
                <Icon icon={project.icon} className="size-5" client:load />
              </span>
              <div>
                <h3 class="text-base font-semibold text-muted-foreground" data-t={project.titleKey}>
                  {project.title}
                </h3>
                <p class="mt-1 text-sm text-muted-foreground/60" data-t={project.descKey}>
                  {project.description}
                </p>
              </div>
            </div>
          </a>
        ) : (
          <a
            href={project.href}
            target="_blank"
            rel="noopener noreferrer"
            class="proj group relative flex overflow-hidden bg-background transition-colors duration-300"
            style={`--accent: ${project.color};`}
          >
            <div class="pointer-events-none absolute inset-0 z-0 opacity-50" style={TEXTURE} aria-hidden="true"></div>

            <div class="relative z-[1] flex flex-1 flex-col p-6">
              <div class="mb-5 flex items-start justify-between">
                <span class="proj-chip flex size-10 items-center justify-center rounded-[3px]">
                  <Icon icon={project.icon} className="size-5" client:load />
                </span>
                <span class="proj-arrow flex size-8 items-center justify-center rounded-[3px] border border-border text-muted-foreground">
                  <Icon icon="lucide:arrow-up-right" className="size-4" client:load />
                </span>
              </div>

              <h3 class="text-lg font-semibold text-foreground" data-t={project.titleKey}>
                {project.title}
              </h3>
              <p class="mt-2 text-sm leading-relaxed text-muted-foreground" data-t={project.descKey}>
                {project.description}
              </p>

              {project.tags.length > 0 && (
                <div class="mt-auto flex flex-wrap gap-1.5 pt-5">
                  {project.tags.map((tag) => (
                    <span class="proj-tag rounded-[2px] border px-2 py-1 font-mono text-[10px] font-semibold uppercase tracking-wide">
                      {tag}
                    </span>
                  ))}
                </div>
              )}
            </div>
          </a>
        )
      ))}
    </div>
  </div>
</section>

<style>
  .proj:hover {
    background: color-mix(in oklch, var(--accent) 5%, transparent);
  }
  .proj-chip {
    background: color-mix(in oklch, var(--accent) 12%, transparent);
    color: var(--accent);
  }
  .proj-tag {
    color: var(--accent);
    border-color: color-mix(in oklch, var(--accent) 30%, transparent);
    background: color-mix(in oklch, var(--accent) 6%, transparent);
  }
  .proj-arrow {
    transition: transform 0.3s ease, color 0.3s ease, border-color 0.3s ease;
  }
  .proj:hover .proj-arrow {
    transform: rotate(45deg);
    color: var(--accent);
    border-color: color-mix(in oklch, var(--accent) 45%, transparent);
  }
</style>
