---
import { getCollection, render, type CollectionEntry } from "astro:content";
import Layout from "@layouts/Layout.astro";

type DocumentChapter = CollectionEntry<"documents">;

type PathParams = {
  document: string;
  chapter: string;
};

export async function getStaticPaths() {
  const chapters = await getCollection("documents");
  const visibleDocuments = await getCollection(
    "documentIndex",
    ({ data }) => !data.draft,
  );
  const visibleDocumentIds = new Set(visibleDocuments.map((doc) => doc.data.document));

  return chapters
    .filter((entry) => visibleDocumentIds.has(entry.data.document))
    .map((entry) => {
      const [document, chapter] = entry.slug.split("/");

      if (!document || !chapter) {
        throw new Error(`Invalid document slug format: ${entry.slug}`);
      }

      return {
        params: { document, chapter },
        props: { entry },
      };
    });
}

const { entry } = Astro.props as { entry: DocumentChapter };
const params = Astro.params as PathParams;

const { Content } = await render(entry);

const [documentIndex] = await getCollection(
  "documentIndex",
  ({ data }) => !data.draft && data.document === entry.data.document,
);

if (!documentIndex) {
  throw new Error(`Missing document index metadata for '${entry.data.document}'.`);
}

const chaptersInDocument = (await getCollection(
  "documents",
  ({ data }) => data.document === entry.data.document,
)).sort((a, b) => a.data.chapter - b.data.chapter);

const currentIndex = chaptersInDocument.findIndex((c) => c.id === entry.id);
if (currentIndex === -1) {
  throw new Error(`Current chapter not found in document list: ${entry.id}`);
}

const previous = chaptersInDocument[currentIndex - 1];
const next = chaptersInDocument[currentIndex + 1];

function chapterLink(chapter: DocumentChapter) {
  const [document, chapterSlug] = chapter.slug.split("/");
  if (!document || !chapterSlug) {
    throw new Error(`Invalid document slug format: ${chapter.slug}`);
  }
  return `/documents/${document}/${chapterSlug}/`;
}

const dateFormatter = new Intl.DateTimeFormat("en-US", { dateStyle: "long" });
---

<Layout
  title={`${entry.data.chapterTitle} — ${documentIndex.data.documentTitle}`}
  description={documentIndex.data.documentDescription}
  canonical={`/documents/${params.document}/${params.chapter}/`}
>
  <main id="main-content" class="doc-page">
    <article class="doc container section" data-animate>
      <p class="doc-back"><a href="/writing/">← Back to writing</a></p>

      <header class="doc__header">
        <p class="doc__eyebrow">{documentIndex.data.documentTitle}</p>
        <h1>{entry.data.chapterTitle}</h1>
        <p class="doc__desc">{documentIndex.data.documentDescription}</p>

        <div class="doc__meta">
          <span>Chapter {entry.data.chapter} of {chaptersInDocument.length}</span>
          <time datetime={entry.data.publishDate.toISOString()}>
            Published {dateFormatter.format(entry.data.publishDate)}
          </time>
          {entry.data.estimatedReadMinutes && <span>{entry.data.estimatedReadMinutes} min read</span>}
        </div>
      </header>

      <div class="doc-body">
        <div class="doc-content">
          <Content />
        </div>

        <aside class="doc-outline" aria-label="Document chapters">
          <h2>Document Outline</h2>
          <p class="doc-outline__hint">Summary follows while you scroll.</p>
          <ol>
            {chaptersInDocument.map((chapter) => (
              <li class={chapter.id === entry.id ? "is-active" : undefined}>
                <a href={chapterLink(chapter)}>
                  {chapter.data.chapter}. {chapter.data.chapterTitle}
                </a>
              </li>
            ))}
          </ol>
        </aside>
      </div>

      <nav class="doc-pagination" aria-label="Chapter navigation">
        <div>
          {previous ? <a href={chapterLink(previous)}>← {previous.data.chapterTitle}</a> : <span />}
        </div>
        <div>
          {next ? <a href={chapterLink(next)}>{next.data.chapterTitle} →</a> : <span />}
        </div>
      </nav>
    </article>
  </main>
</Layout>

<style>
  .doc-page {
    min-height: 70vh;
  }

  .doc {
    max-width: 80rem;
    position: relative;
    overflow: visible;
  }

  .doc-back {
    margin-bottom: 1rem;
  }

  .doc-back a {
    color: var(--color-accent-blue);
    font-weight: 500;
  }

  .doc__header {
    margin-bottom: 1.5rem;
  }

  .doc__eyebrow {
    color: var(--color-accent-blue);
    font-size: var(--font-size-sm);
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .doc__header h1 {
    margin-top: 0.5rem;
    font-size: clamp(1.8rem, 4.5vw, 3rem);
  }

  .doc__desc {
    margin-top: 0.7rem;
    font-size: var(--font-size-lg);
  }

  .doc__meta {
    margin-top: 1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    color: var(--color-ink-muted);
    font-size: var(--font-size-sm);
  }

  .doc-body {
    display: grid;
    gap: 1.25rem;
  }

  .doc-outline {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: var(--color-surface);
    padding: 1rem;
    margin-bottom: 1.5rem;
  }

  .doc-outline h2 {
    font-size: var(--font-size-base);
    margin-bottom: 0.6rem;
  }

  .doc-outline__hint {
    margin-bottom: 0.7rem;
    font-size: var(--font-size-xs);
    color: var(--color-ink-muted);
  }

  .doc-outline ol {
    margin: 0;
    padding-left: 1.2rem;
    display: grid;
    gap: 0.3rem;
  }

  .doc-outline li {
    color: var(--color-ink-secondary);
  }

  .doc-outline li.is-active a {
    color: var(--color-accent-blue);
    font-weight: 600;
  }

  .doc-content :global(h2),
  .doc-content :global(h3),
  .doc-content :global(h4) {
    margin-top: 1.8rem;
    margin-bottom: 0.7rem;
  }

  .doc-content :global(h2) {
    font-size: clamp(1.5rem, 2.4vw, 1.95rem);
    line-height: 1.2;
  }

  .doc-content :global(h3) {
    font-size: clamp(1.2rem, 1.8vw, 1.45rem);
    line-height: 1.28;
  }

  .doc-content :global(h4) {
    font-size: clamp(1.05rem, 1.4vw, 1.18rem);
    line-height: 1.32;
  }

  .doc-content :global(p),
  .doc-content :global(ul),
  .doc-content :global(ol),
  .doc-content :global(blockquote) {
    margin-bottom: 1rem;
  }

  .doc-content :global(p) {
    font-size: 1.03rem;
    line-height: 1.72;
  }

  .doc-content :global(ul),
  .doc-content :global(ol) {
    padding-left: 1.45rem;
  }

  .doc-content :global(ul) { list-style-type: disc; }
  .doc-content :global(ol) { list-style-type: decimal; }
  .doc-content :global(ul ul) { list-style-type: circle; }
  .doc-content :global(ul ul ul) { list-style-type: square; }
  .doc-content :global(ol ol) { list-style-type: lower-alpha; }

  .doc-content :global(li) {
    margin-bottom: 0.45rem;
    line-height: 1.68;
  }

  .doc-content :global(li > ul),
  .doc-content :global(li > ol) {
    margin-top: 0.4rem;
    margin-bottom: 0.4rem;
    padding-left: 1.2rem;
  }

  .doc-content :global(blockquote) {
    border-left: 3px solid var(--color-border);
    padding-left: 0.9rem;
    color: var(--color-ink-secondary);
  }

  .doc-content :global(code) {
    font-family: var(--font-family-mono);
    font-size: 0.865em;
    background: #e8e7e1;
    color: var(--color-ink-primary);
    padding: 0.15rem 0.42rem;
    border-radius: var(--radius-sm);
    border: 1px solid #d4d3cc;
    font-weight: var(--font-weight-medium);
  }

  .doc-content :global(pre) {
    margin-bottom: 1rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    background: #f0f0ed;
    padding: 1rem;
    overflow-x: auto;
  }

  .doc-content :global(pre code) {
    background: transparent;
    padding: 0;
    border: none;
    font-weight: var(--font-weight-normal);
  }

  .doc-pagination {
    margin-top: 2rem;
    padding-top: 1rem;
    border-top: 1px solid var(--color-border);
    display: flex;
    justify-content: space-between;
    gap: 1rem;
  }

  .doc-pagination a {
    color: var(--color-accent-blue);
    font-weight: 500;
  }

  @media (min-width: 1024px) {
    .doc-body {
      grid-template-columns: minmax(0, 56rem) 20rem;
      align-items: start;
      justify-content: center;
    }

    .doc-content {
      max-width: 56rem;
    }

    .doc-outline {
      position: sticky;
      top: 6.5rem;
      width: 20rem;
      max-height: calc(100vh - 8rem);
      overflow: auto;
      z-index: 1;
      margin-bottom: 0;
    }
  }
</style>
