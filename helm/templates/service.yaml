apiVersion: v1
kind: Service
metadata:
  name: {{ include "bonnal-cloud.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "bonnal-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bonnal-cloud.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
